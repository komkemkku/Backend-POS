# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_DATABASE=pos_db
DB_USER=postgres
DB_PASSWORD=your_password_here

# Server Configuration
PORT=8080
GIN_MODE=release

# JWT Configuration
TOKEN_SECRET=your_jwt_secret_here_minimum_32_characters
TOKEN_DURATION=24h

# Railway Production Environment
# Set GIN_MODE=release for production
# Railway will automatically set PORT
# Use Railway's PostgreSQL service variables
